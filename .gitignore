node_modules/
.next/
out/
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Native projects are generated by `npm run native:prepare` (and by CI) from
# capacitor.config.ts and assets/, so they are not committed. Capacitor's own
# templates add their ignore files here once generated.
android/
ios/
*.apk
*.ipa
*.aab

# Local build output and emulator evidence (not shipped).
dist/
